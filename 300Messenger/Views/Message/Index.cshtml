@model IEnumerable<MessageSession>

@{
    ViewData["Title"] = "Messages";
}

@foreach (var message in Model)
{
    var cutContent = message.Content.Length > 20 ? $"{message.Content.Substring(0, 20)}..." : message.Content;
    <p>From: @message.Email:
        <a asp-controller="Message" asp-action="Details" asp-route-id="@message.Id">
            @cutContent
    </a></p>
    <hr>
}
<hr />
<div>
    <h5><a asp-controller="Message" asp-action="Create">Post New Message</a></h5>
</div>
